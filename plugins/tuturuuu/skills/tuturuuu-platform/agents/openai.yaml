interface:
  display_name: "Tuturuuu Platform"
  short_description: "Work safely in the Tuturuuu platform shared worktree"
  default_prompt: "Make this platform change in the shared worktree and run the checks it needs."
